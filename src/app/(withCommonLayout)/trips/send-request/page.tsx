const SendRequestPage = ({ searchParams }: { searchParams: any }) => {
	const tripId = searchParams?.tripId;

	return (
		<div>
			<h1>This is SendRequestPage component</h1>
		</div>
	);
};

export default SendRequestPage;
